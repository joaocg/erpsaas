<?php

namespace Filament\Support\Concerns;

trait HasIconSize
{
    /*
     * Filament v3 already includes icon size helpers inside the HasIcon trait.
     * Keeping this trait empty avoids method collisions while satisfying
     * packages that expect the separate concern to exist in newer versions.
     */
}
